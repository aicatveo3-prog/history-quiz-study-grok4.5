/* 앱 식별자 — 새 퀴즈 앱을 만들 때 이 파일의 값 하나만 바꾸면 된다.
 * ─────────────────────────────────────────────────────────────
 * 여러 퀴즈 앱이 같은 도메인(aicatveo3-prog.github.io)에 배포되므로
 * localStorage 를 공유한다. 앱마다 APP_ID 가 다르면
 *   · localStorage : q:<APP_ID>:*            (키 충돌 없음)
 *   · Firestore    : users/{uid}/apps/<APP_ID>  (앱별 문서로 분리)
 * 로 자동 격리되어, 로그인 계정(uid)은 앱끼리 공유하면서 기록은 섞이지 않는다.
 *
 * ⚠ 한 번 정한 값은 바꾸지 않는다 — 바꾸면 기존 학습 기록을 찾지 못한다.
 */
window.APP_ID = "hanguksa";
